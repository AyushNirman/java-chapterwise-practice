public class MoveZerosToEnd {

    public static void moveZeroes(int[] arr) {

        // index will store position for next non-zero element
        int index = 0;

        // Step 1: Move all non-zero elements to the front
        for (int i = 0; i < arr.length; i++) {

            // If current element is not zero
            if (arr[i] != 0) {

                // Place it at the 'index' position
                arr[index] = arr[i];

                // Move index to next position
                index++;
            }
        }

        // Step 2: Fill remaining positions with 0
        while (index < arr.length) {
            arr[index] = 0;
            index++;
        }
    }

    public static void main(String[] args) {
        int[] arr = {1, 0, 2, 0, 3};
        moveZeroes(arr);

        // Print result
        for (int num : arr) {
            System.out.print(num + " ");
        }
    }
}


6️⃣ Time complexity?

Answer:
O(n) — array ek hi baar traverse hota hai.

7️⃣ Space complexity?

Answer:
O(1) — koi extra array ya data structure use nahi kiya.       






🔄 Advanced Follow-ups (Sometimes Asked)
11️⃣ Modify solution to move all negative numbers to end

Answer:
Same logic, bas condition change:

if (arr[i] >= 0) 

public class Main {

    // Method to remove duplicates from sorted array
    static int removeDuplicates(int[] arr) {

        // Edge case: empty array
        if (arr.length == 0) return 0;

        int j = 0; // index of last unique element

        // Traverse array from second element
        for (int i = 1; i < arr.length; i++) {

            // If new element is different, it's unique
            if (arr[i] != arr[j]) {
                arr[++j] = arr[i]; // place unique element ahead
            }
        }

        // Return count of unique elements
        return j + 1;
    }

    public static void main(String[] args) {

        int[] arr = {1, 1, 2, 2, 3, 4, 4};

        int len = removeDuplicates(arr);

        // Print only unique elements
        for (int i = 0; i < len; i++) {
            System.out.print(arr[i] + " ");
        }
    }
}




⏱ Time & Space Complexity
Time Complexity

Single loop → O(n)

Space Complexity

No extra array → O(1)

🗣️

“This is an in-place solution with optimal time and space complexity.”



🎯 Interviewer Follow-Up Questions (VERY IMPORTANT)
1️⃣ Why array must be sorted?

🗣️

“Because duplicates are adjacent, which makes comparison easy.”

2️⃣ What if array is not sorted?

🗣️

“We need sorting or extra space, otherwise time complexity becomes O(n²).”

3️⃣ Why return length instead of new array?

🗣️

“To avoid extra memory and follow in-place modification.”

4️⃣ What is the two-pointer technique?

🗣️

“Using two indices to traverse and modify data efficiently in one pass.”

5️⃣ Edge cases?

🗣️

Empty array

Single element array







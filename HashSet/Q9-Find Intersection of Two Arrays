import java.util.*;

public class Main {
    public static void main(String[] args) {

        int[] a = {1, 2, 3, 4};
        int[] b = {3, 4, 5, 6};

        HashSet<Integer> set = new HashSet<>();
        HashSet<Integer> result = new HashSet<>();

        for (int x : a) {
            set.add(x);
        }

        for (int y : b) {
            if (set.contains(y)) {
                result.add(y);
            }
        }

        System.out.println(result);
    }
}

Store first array in HashSet

Check second array

If element exists → common element
